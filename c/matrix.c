#include <stdio.h>
int main() {
        int a,b;
        printf("Enter two integer values seperated by space :");
        scanf("%d %d",&a,&b);
        printf("The value of %d + %d = %d \n", a,b,a+b);
        printf("The value of %d * %d = %d \n", a,b,a*b);
        return 0;
}
